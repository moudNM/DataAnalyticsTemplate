Install all libs using commands below, to use

pip install pandas
pip install matplotlib
pip install openpyxl
pip install seaborn
pip install sklearn

(using python3 so to run a file from command line, use python3 filename.py)